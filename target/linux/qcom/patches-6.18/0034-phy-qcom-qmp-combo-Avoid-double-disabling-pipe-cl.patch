--- a/drivers/phy/qualcomm/phy-qcom-qmp-combo.c
+++ b/drivers/phy/qualcomm/phy-qcom-qmp-combo.c
@@ -3281,10 +3281,8 @@ static int qmp_combo_usb_init(struct phy
 		goto out_unlock;
 
 	ret = qmp_combo_usb_power_on(phy);
-	if (ret) {
-		qmp_combo_com_exit(qmp, false);
+	if (ret)
 		goto out_unlock;
-	}
 
 	qmp->usb_init_count++;
 
