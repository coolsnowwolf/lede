bin_PROGRAMS = md5test \
		$(NULL)
md5test_SOURCES = md5test.c \
		md5_one.c \
		md5_dgst.c \
		mem_clr.c \
		$(NULL)
md5test_CFLAGS = $(NULL)
md5test_CFLAGS += $(libcrypto_CFLAGS)
md5test_LDADD = $(NULL)
md5test_LDADD += $(NULL)

