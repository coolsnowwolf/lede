From: coolsnowwolf <coolsnowwolf@gmail.com>
Subject: [PATCH] phytium: gpu linux 6.18 compat

Signed-off-by: coolsnowwolf <coolsnowwolf@gmail.com>

--- a/gpu_driver/Kbuild
+++ b/gpu_driver/Kbuild
@@ -82,12 +82,12 @@
 -include $(AQROOT)/$(SOC_PLATFORM_LOCATION)/gc_hal_kernel_platform_$(soc_board).config
 
 ifeq ($(CONFIG_KASAN),)
-EXTRA_CFLAGS += -Werror -Wno-implicit-fallthrough
+ccflags-y += -Werror -Wno-implicit-fallthrough
 ccflags-$(CONFIG_WERROR) += -Werror
 endif
 
-EXTRA_CFLAGS += -Werror -Wno-attributes
-EXTRA_CFLAGS += -Werror -Wno-missing-attributes
+ccflags-y += -Werror -Wno-attributes
+ccflags-y += -Werror -Wno-missing-attributes
 
 OBJS += $(OS_KERNEL_DIR)/gc_hal_kernel_device.o \
         $(OS_KERNEL_DIR)/gc_hal_kernel_linux.o \
@@ -134,13 +134,13 @@
 OBJS += $(ARCH_KERNEL_DIR)/gc_hal_kernel_recorder.o
 
 ifneq ($(CONFIG_SYNC),)
-EXTRA_CFLAGS += -Idrivers/staging/android
-EXTRA_CFLAGS += -DgcdLINUX_SYNC_FILE=1
+ccflags-y += -Idrivers/staging/android
+ccflags-y += -DgcdLINUX_SYNC_FILE=1
 
 OBJS += $(OS_KERNEL_DIR)/gc_hal_kernel_sync.o
 else
   ifneq ($(CONFIG_SYNC_FILE),)
-  EXTRA_CFLAGS += -DgcdLINUX_SYNC_FILE=1
+  ccflags-y += -DgcdLINUX_SYNC_FILE=1
   OBJS += $(OS_KERNEL_DIR)/gc_hal_kernel_sync.o
   endif
 endif
@@ -163,120 +163,120 @@
         $(TA_DIR)/os/emulator/gc_hal_ta_emulator.o
 endif
 
-EXTRA_CFLAGS += -DLINUX -DDRIVER
+ccflags-y += -DLINUX -DDRIVER
 
 ifeq ($(FLAREON),1)
-EXTRA_CFLAGS += -DFLAREON
+ccflags-y += -DFLAREON
 endif
 
 ifeq ($(DEBUG), 1)
-EXTRA_CFLAGS += -DDBG=1 -DDEBUG -D_DEBUG -g
+ccflags-y += -DDBG=1 -DDEBUG -D_DEBUG -g
 else
-EXTRA_CFLAGS += -DDBG=0
+ccflags-y += -DDBG=0
 endif
 
 ifeq ($(NO_DMA_COHERENT), 1)
-EXTRA_CFLAGS += -DNO_DMA_COHERENT
+ccflags-y += -DNO_DMA_COHERENT
 endif
 
 ifeq ($(CONFIG_DOVE_GPU), 1)
-EXTRA_CFLAGS += -DCONFIG_DOVE_GPU=1
+ccflags-y += -DCONFIG_DOVE_GPU=1
 endif
 
 ifneq ($(USE_PLATFORM_DRIVER), 0)
-EXTRA_CFLAGS += -DUSE_PLATFORM_DRIVER=1
+ccflags-y += -DUSE_PLATFORM_DRIVER=1
 else
-EXTRA_CFLAGS += -DUSE_PLATFORM_DRIVER=0
+ccflags-y += -DUSE_PLATFORM_DRIVER=0
 endif
 
-EXTRA_CFLAGS += -DPHYTIUM_PROFILER_CONTEXT=1
+ccflags-y += -DPHYTIUM_PROFILER_CONTEXT=1
 
 ifeq ($(ENABLE_GPU_CLOCK_BY_DRIVER), 1)
-EXTRA_CFLAGS += -DENABLE_GPU_CLOCK_BY_DRIVER=1
+ccflags-y += -DENABLE_GPU_CLOCK_BY_DRIVER=1
 else
-EXTRA_CFLAGS += -DENABLE_GPU_CLOCK_BY_DRIVER=0
+ccflags-y += -DENABLE_GPU_CLOCK_BY_DRIVER=0
 endif
 
 ifeq ($(USE_NEW_LINUX_SIGNAL), 1)
-EXTRA_CFLAGS += -DUSE_NEW_LINUX_SIGNAL=1
+ccflags-y += -DUSE_NEW_LINUX_SIGNAL=1
 else
-EXTRA_CFLAGS += -DUSE_NEW_LINUX_SIGNAL=0
+ccflags-y += -DUSE_NEW_LINUX_SIGNAL=0
 endif
 
 ifeq ($(USE_LINUX_PCIE), 1)
-EXTRA_CFLAGS += -DUSE_LINUX_PCIE=1
+ccflags-y += -DUSE_LINUX_PCIE=1
 else
-EXTRA_CFLAGS += -DUSE_LINUX_PCIE=0
+ccflags-y += -DUSE_LINUX_PCIE=0
 endif
 
 ifeq ($(CACHE_FUNCTION_UNIMPLEMENTED), 1)
-EXTRA_CFLAGS += -DgcdCACHE_FUNCTION_UNIMPLEMENTED=1
+ccflags-y += -DgcdCACHE_FUNCTION_UNIMPLEMENTED=1
 else
-EXTRA_CFLAGS += -DgcdCACHE_FUNCTION_UNIMPLEMENTED=0
+ccflags-y += -DgcdCACHE_FUNCTION_UNIMPLEMENTED=0
 endif
 
-EXTRA_CFLAGS += -DgcdENABLE_3D=1
+ccflags-y += -DgcdENABLE_3D=1
 
 
 ifeq ($(USE_BANK_ALIGNMENT), 1)
-    EXTRA_CFLAGS += -DgcdENABLE_BANK_ALIGNMENT=1
+    ccflags-y += -DgcdENABLE_BANK_ALIGNMENT=1
     ifneq ($(BANK_BIT_START), 0)
         ifneq ($(BANK_BIT_END), 0)
-            EXTRA_CFLAGS += -DgcdBANK_BIT_START=$(BANK_BIT_START)
-            EXTRA_CFLAGS += -DgcdBANK_BIT_END=$(BANK_BIT_END)
+            ccflags-y += -DgcdBANK_BIT_START=$(BANK_BIT_START)
+            ccflags-y += -DgcdBANK_BIT_END=$(BANK_BIT_END)
         endif
     endif
 
     ifneq ($(BANK_CHANNEL_BIT), 0)
-        EXTRA_CFLAGS += -DgcdBANK_CHANNEL_BIT=$(BANK_CHANNEL_BIT)
+        ccflags-y += -DgcdBANK_CHANNEL_BIT=$(BANK_CHANNEL_BIT)
     endif
 endif
 
 ifeq ($(FPGA_BUILD), 1)
-EXTRA_CFLAGS += -DgcdFPGA_BUILD=1
+ccflags-y += -DgcdFPGA_BUILD=1
 else
-EXTRA_CFLAGS += -DgcdFPGA_BUILD=0
+ccflags-y += -DgcdFPGA_BUILD=0
 endif
 
 ifeq ($(SECURITY), 1)
-EXTRA_CFLAGS += -DgcdSECURITY=1
+ccflags-y += -DgcdSECURITY=1
 endif
 
 ifneq ($(CONFIG_DRM), )
-EXTRA_CFLAGS += -DgcdENABLE_DRM=$(PHYTIUM_ENABLE_DRM)
+ccflags-y += -DgcdENABLE_DRM=$(PHYTIUM_ENABLE_DRM)
 else
-EXTRA_CFLAGS += -DgcdENABLE_DRM=0
+ccflags-y += -DgcdENABLE_DRM=0
 endif
 
-EXTRA_CFLAGS += -I$(AQROOT)/hal/inc
-EXTRA_CFLAGS += -I$(AQROOT)/hal/kernel
-EXTRA_CFLAGS += -I$(AQROOT)/hal/kernel/arch
-EXTRA_CFLAGS += -I$(AQARCH)/cmodel/inc
-EXTRA_CFLAGS += -I$(AQROOT)/hal/user
-EXTRA_CFLAGS += -I$(AQROOT)/hal/os/linux/kernel
-EXTRA_CFLAGS += -I$(AQROOT)/$(ALLOCATOR_ARRAY_H_LOCATION)
-EXTRA_CFLAGS += -I$(AQROOT)/$(SOC_PLATFORM_LOCATION)
+ccflags-y += -I$(AQROOT)/hal/inc
+ccflags-y += -I$(AQROOT)/hal/kernel
+ccflags-y += -I$(AQROOT)/hal/kernel/arch
+ccflags-y += -I$(AQARCH)/cmodel/inc
+ccflags-y += -I$(AQROOT)/hal/user
+ccflags-y += -I$(AQROOT)/hal/os/linux/kernel
+ccflags-y += -I$(AQROOT)/$(ALLOCATOR_ARRAY_H_LOCATION)
+ccflags-y += -I$(AQROOT)/$(SOC_PLATFORM_LOCATION)
 
 ifeq ($(ENABLE_TRUST_APPLICATION), 1)
-EXTRA_CFLAGS += -I$(AQROOT)/hal/security_v1/
+ccflags-y += -I$(AQROOT)/hal/security_v1/
 endif
 
 ifneq ($(CONFIG_ARM), )
-EXTRA_CFLAGS += -Iarch/arm/mm
+ccflags-y += -Iarch/arm/mm
 endif
 
-EXTRA_CFLAGS += -DHOST=\"$(HOST)\"
+ccflags-y += -DHOST=\"$(HOST)\"
 
 ifeq ($(ENABLE_TRUST_APPLICATION), 1)
-EXTRA_CFLAGS += -DgcdENABLE_TRUST_APPLICATION=1
+ccflags-y += -DgcdENABLE_TRUST_APPLICATION=1
 endif
 
 ifeq ($(ENABLE_VIDEO_MEMORY_MIRROR), 1)
-EXTRA_CFLAGS += -DgcdENABLE_VIDEO_MEMORY_MIRROR=1
+ccflags-y += -DgcdENABLE_VIDEO_MEMORY_MIRROR=1
 endif
 
 ifeq ($(ENABLE_VM_PASSTHROUGH),1)
-EXTRA_CFLAGS += -DgcdENABLE_VM_PASSTHROUGH=1
+ccflags-y += -DgcdENABLE_VM_PASSTHROUGH=1
 endif
 
 obj-m = $(MODULE_NAME).o
--- a/gpu_driver/hal/kernel/gc_hal_kernel_mmu.c
+++ b/gpu_driver/hal/kernel/gc_hal_kernel_mmu.c
@@ -784,8 +784,9 @@
             stlbLogical = (gctUINT32_PTR)((gctUINT8_PTR)curStlbChunk->logical +
                                           (stlbOffset * gcdMMU_STLB_16M_SIZE));
 
-            if (stlbPhyBase != (mtlbEntry & gcdMMU_MTLB_ENTRY_STLB_MASK))
+            if (stlbPhyBase != (mtlbEntry & gcdMMU_MTLB_ENTRY_STLB_MASK)) {
                 gcmkASSERT(0);
+            }
         }
 
 #if gcdDUMP_IN_KERNEL
@@ -1189,8 +1190,9 @@
             stlbLogical = (gctUINT32_PTR)((gctUINT8_PTR)curStlbChunk->logical +
                                           (stlbOffset * gcdMMU_STLB_1M_SIZE));
 
-            if (stlbPhyBase != (mtlbEntry & gcdMMU_MTLB_ENTRY_STLB_MASK))
+            if (stlbPhyBase != (mtlbEntry & gcdMMU_MTLB_ENTRY_STLB_MASK)) {
                 gcmkASSERT(0);
+            }
         }
 
 #if gcdDUMP_IN_KERNEL
@@ -1502,8 +1504,9 @@
             stlbLogical = (gctUINT32_PTR)((gctUINT8_PTR)curStlbChunk->logical +
                                           (stlbOffset * gcdMMU_STLB_64K_SIZE));
 
-            if (stlbPhyBase != (mtlbEntry & gcdMMU_MTLB_ENTRY_STLB_MASK))
+            if (stlbPhyBase != (mtlbEntry & gcdMMU_MTLB_ENTRY_STLB_MASK)) {
                 gcmkASSERT(0);
+            }
         }
 
 #if gcdDUMP_IN_KERNEL
@@ -1817,8 +1820,9 @@
             stlbLogical = (gctUINT32_PTR)((gctUINT8_PTR)curStlbChunk->logical +
                                           (stlbOffset * gcdMMU_STLB_4K_SIZE));
 
-            if (stlbPhyBase != (mtlbEntry & gcdMMU_MTLB_ENTRY_STLB_MASK))
+            if (stlbPhyBase != (mtlbEntry & gcdMMU_MTLB_ENTRY_STLB_MASK)) {
                 gcmkASSERT(0);
+            }
         }
 
 #if gcdDUMP_IN_KERNEL
--- a/gpu_driver/hal/os/linux/kernel/allocator/default/gc_hal_kernel_allocator_dma.c
+++ b/gpu_driver/hal/os/linux/kernel/allocator/default/gc_hal_kernel_allocator_dma.c
@@ -246,7 +246,7 @@
 # endif
 
         for (i = 0; i < numPages; ++i)
-            pages[i] = nth_page(page, i + skipPages);
+            pages[i] = pfn_to_page(page_to_pfn(page) + i + skipPages);
     }
 
     if (sg_alloc_table_from_pages(sgt, pages, numPages, offset, Bytes, GFP_KERNEL) < 0)
--- a/gpu_driver/hal/os/linux/kernel/allocator/default/gc_hal_kernel_allocator_dmabuf.c
+++ b/gpu_driver/hal/os/linux/kernel/allocator/default/gc_hal_kernel_allocator_dmabuf.c
@@ -73,8 +73,12 @@
 #define _GC_OBJ_ZONE gcvZONE_OS
 
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 16, 0)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 13, 0)
+MODULE_IMPORT_NS("DMA_BUF");
+#else
 MODULE_IMPORT_NS(DMA_BUF);
 #endif
+#endif
 
 /* Descriptor of a dma_buf imported. */
 typedef struct _gcsDMABUF {
--- a/gpu_driver/hal/os/linux/kernel/allocator/default/gc_hal_kernel_allocator_gfp.c
+++ b/gpu_driver/hal/os/linux/kernel/allocator/default/gc_hal_kernel_allocator_gfp.c
@@ -368,7 +368,7 @@
         MdlPriv->numPages1M += 1;
 
         for (j = 0; j < num; j++) {
-            page = nth_page(MdlPriv->Pages1M[i], j);
+            page = pfn_to_page(page_to_pfn(MdlPriv->Pages1M[i]) + j);
             pages[i * num + j] = page;
         }
     }
@@ -595,7 +595,7 @@
         struct page *page;
 
         if (contiguous)
-            page = nth_page(mdlPriv->contiguousPages, i);
+            page = pfn_to_page(page_to_pfn(mdlPriv->contiguousPages) + i);
         else
             page = mdlPriv->nonContiguousPages[i];
 
@@ -647,7 +647,8 @@
             gcmkONERROR(gcvSTATUS_OUT_OF_MEMORY);
 
         for (i = 0; i < numPages; ++i)
-            pages[i] = nth_page(mdlPriv->contiguousPages, i + skipPages);
+            pages[i] = pfn_to_page(page_to_pfn(mdlPriv->contiguousPages) +
+                                   i + skipPages);
     } else {
         pages = &mdlPriv->nonContiguousPages[skipPages];
     }
@@ -701,7 +702,7 @@
 
     for (i = 0; i < Mdl->numPages; i++) {
         if (mdlPriv->contiguous)
-            page = nth_page(mdlPriv->contiguousPages, i);
+            page = pfn_to_page(page_to_pfn(mdlPriv->contiguousPages) + i);
         else
             page = mdlPriv->nonContiguousPages[i];
 
@@ -951,7 +952,8 @@
             return gcvSTATUS_OUT_OF_MEMORY;
 
         for (i = 0; i < numPages; i++)
-            pages[i] = nth_page(mdlPriv->contiguousPages, i + pgoff);
+            pages[i] = pfn_to_page(page_to_pfn(mdlPriv->contiguousPages) +
+                                   i + pgoff);
 
         free = gcvTRUE;
     } else {
@@ -1070,7 +1072,8 @@
     gctUINT32 index = (gctUINT32)(Offset / PAGE_SIZE);
 
     if (mdlPriv->contiguous)
-        *Physical = page_to_phys(nth_page(mdlPriv->contiguousPages, index));
+        *Physical = page_to_phys(pfn_to_page(
+            page_to_pfn(mdlPriv->contiguousPages) + index));
     else
         *Physical = page_to_phys(mdlPriv->nonContiguousPages[index]);
 
--- a/gpu_driver/hal/os/linux/kernel/allocator/default/gc_hal_kernel_allocator_user_memory.c
+++ b/gpu_driver/hal/os/linux/kernel/allocator/default/gc_hal_kernel_allocator_user_memory.c
@@ -350,15 +350,6 @@
     if (!current->mm)
         return -ENOTTY;
 
-    down_read(&current_mm_mmap_sem);
-    vma = find_vma(current->mm, addr);
-#if !gcdUSING_PFN_FOLLOW && (LINUX_VERSION_CODE < KERNEL_VERSION(6, 5, 0))
-    up_read(&current_mm_mmap_sem);
-#endif
-
-    if (!vma)
-        return -ENOTTY;
-
     pfns = (unsigned long *)alloc_memory(pfn_count * sizeof(unsigned long));
 
     if (!pfns)
@@ -378,8 +369,37 @@
         return -ENOMEM;
     }
 
+    down_read(&current_mm_mmap_sem);
+    vma = find_vma(current->mm, addr);
+    if (!vma) {
+        up_read(&current_mm_mmap_sem);
+        goto err;
+    }
+#if !gcdUSING_PFN_FOLLOW && \
+	(LINUX_VERSION_CODE < KERNEL_VERSION(6, 5, 0) || \
+	 (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 10, 0) && \
+	  LINUX_VERSION_CODE < KERNEL_VERSION(6, 12, 0)))
+    up_read(&current_mm_mmap_sem);
+#endif
+
     for (i = 0; i < pfn_count; i++) {
-#if gcdUSING_PFN_FOLLOW || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0))
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0)
+        struct follow_pfnmap_args args = {
+            .vma = vma,
+            .address = addr,
+        };
+        int ret;
+
+        ret = follow_pfnmap_start(&args);
+        if (ret < 0) {
+            up_read(&current_mm_mmap_sem);
+            goto err;
+        }
+        pfns[i] = args.pfn;
+        follow_pfnmap_end(&args);
+#elif gcdUSING_PFN_FOLLOW || \
+      (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0) && \
+       LINUX_VERSION_CODE < KERNEL_VERSION(6, 10, 0))
         int ret = 0;
         ret = follow_pfn(vma, addr, &pfns[i]);
         if (ret < 0) {
@@ -434,7 +454,10 @@
         /* Advance to next. */
         addr += PAGE_SIZE;
     }
-#if gcdUSING_PFN_FOLLOW || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0))
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0) || \
+    gcdUSING_PFN_FOLLOW || \
+    (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0) && \
+     LINUX_VERSION_CODE < KERNEL_VERSION(6, 10, 0))
     up_read(&current_mm_mmap_sem);
 #endif
 
--- a/gpu_driver/hal/os/linux/kernel/gc_hal_kernel_device.c
+++ b/gpu_driver/hal/os/linux/kernel/gc_hal_kernel_device.c
@@ -3727,7 +3727,7 @@
 
 struct devfreq *devf;
 
-gceSTATUS
+static gceSTATUS
 _EnableDevfreq(gckGALDEVICE Device)
 {
     struct ftg_devfreq *private;
--- a/gpu_driver/hal/os/linux/kernel/gc_hal_kernel_driver.c
+++ b/gpu_driver/hal/os/linux/kernel/gc_hal_kernel_driver.c
@@ -71,8 +71,12 @@
 MODULE_VERSION(gcvVERSION_STRING);
 MODULE_LICENSE("Dual MIT/GPL");
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 4, 0)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 13, 0)
+MODULE_IMPORT_NS("VFS_internal_I_am_really_a_filesystem_and_am_NOT_a_driver");
+#else
 MODULE_IMPORT_NS(VFS_internal_I_am_really_a_filesystem_and_am_NOT_a_driver);
 #endif
+#endif
 
 static struct class *gpu_class = gcvNULL;
 
@@ -1197,7 +1201,9 @@
     return ret;
 }
 
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(3, 8, 0)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 11, 0)
+static void viv_dev_remove(struct platform_device *pdev)
+#elif LINUX_VERSION_CODE >= KERNEL_VERSION(3, 8, 0)
 static int viv_dev_remove(struct platform_device *pdev)
 #else
 static int __devexit viv_dev_remove(struct platform_device *pdev)
@@ -1225,7 +1231,9 @@
     }
 
     gcmkFOOTER_NO();
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 11, 0)
     return 0;
+#endif
 }
 
 static void viv_dev_shutdown(struct platform_device *pdev)
--- a/gpu_driver/hal/os/linux/kernel/gc_hal_kernel_drm.c
+++ b/gpu_driver/hal/os/linux/kernel/gc_hal_kernel_drm.c
@@ -787,7 +787,7 @@
 #endif
     .poll               = drm_poll,
     .read               = drm_read,
-    .llseek             = no_llseek,
+    .llseek             = noop_llseek,
 };
 
 static struct drm_driver viv_drm_driver = {
@@ -818,7 +818,9 @@
     .fops               = &viv_drm_fops,
     .name               = "ftg340",
     .desc               = "ftg340 DRM",
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 14, 0)
     .date               = "20170808",
+#endif
     .major              = 1,
     .minor              = 0,
 };
--- a/gpu_driver/hal/os/linux/kernel/gc_hal_kernel_os.c
+++ b/gpu_driver/hal/os/linux/kernel/gc_hal_kernel_os.c
@@ -431,11 +431,22 @@
         /* Kernel logical address. */
         *Address = virt_to_phys(Logical);
         return gcvSTATUS_OK;
-#if gcdUSING_PFN_FOLLOW || (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0))
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0) || \
+    gcdUSING_PFN_FOLLOW || \
+    (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0) && \
+     LINUX_VERSION_CODE < KERNEL_VERSION(6, 10, 0))
     } else {
         struct vm_area_struct *vma;
         unsigned long pfn = 0;
         int ret = 0;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0)
+        struct follow_pfnmap_args args = {
+            .address = logical,
+        };
+#endif
+
+        if (!current->mm)
+            return gcvSTATUS_NOT_FOUND;
 
         down_read(&current_mm_mmap_sem);
         vma = find_vma(current->mm, logical);
@@ -443,8 +454,17 @@
             up_read(&current_mm_mmap_sem);
             return gcvSTATUS_NOT_FOUND;
         }
-        up_read(&current_mm_mmap_sem);
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0)
+        args.vma = vma;
+        ret = follow_pfnmap_start(&args);
+        if (!ret) {
+            pfn = args.pfn;
+            follow_pfnmap_end(&args);
+        }
+#else
         ret = follow_pfn(vma, logical, &pfn);
+#endif
+        up_read(&current_mm_mmap_sem);
         if (ret < 0) {
             return gcvSTATUS_NOT_FOUND;
         } else {
@@ -2306,7 +2326,6 @@
         if (pfn_valid(pfn)) {
             gctUINT32 offset = physical & ~PAGE_MASK;
             struct page **pages;
-            struct page *page;
             gctSIZE_T numPages;
             gctSIZE_T i;
             pgprot_t pgprot;
@@ -2317,10 +2336,8 @@
             if (!pages)
                 gcmkONERROR(gcvSTATUS_OUT_OF_MEMORY);
 
-            page = pfn_to_page(pfn);
-
             for (i = 0; i < numPages; i++)
-                pages[i] = nth_page(page, i);
+                pages[i] = pfn_to_page(pfn + i);
 
 #if gcdENABLE_BUFFERABLE_VIDEO_MEMORY
             pgprot = pgprot_writecombine(PAGE_KERNEL);
